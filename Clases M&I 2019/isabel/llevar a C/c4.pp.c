#include <iostream>
#include<string.h>
using namespace std;
main(){
    char horaP[20],horaLL[20],;

    int c,cmay=0;
    double por;

    cout << "hora de partida : ";
    cin >>horaP;
    cout << "hora de llegada : ";
    cin >> horaLL;

    hp = strtok(horaP, ":");
    mp = strtok (NULL, ";");

    hl = strtok(horaLL, ":");
    ml = strtok (NULL, ";");
    }
    cout <<hp<<hl<<mp<<ml;
}

